name: Sync Page From Google Sheets

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled automation
        run: |
          echo "Scheduled GitHub Actions sync is disabled."
          echo "This repository now uses Codex-connected private Google Sheets reads."
          echo "Run updates from Codex when responses change."
